{% extends 'base.html.twig' %}

{% block body %}

    {% include 'header.html.twig' %}


    <div class="container">
        <div class="jumbotron">
            <h1>Catalogue de Noël</h1>
        </div>

        <div class="row">
            {% for produit in produits %}
                <div class="col-xs-6 col-md-3">
                    <div class="thumbnail">
                        <a href="{{ path('produit_show', { 'id': produit.id }) }}">
                            <img class="img-responsive" src="/image/{{ produit.image }}">
                            <h3>{{ produit.nom }}</h3>
                            <a class="btn btn-default glyphicon glyphicon-shopping-cart"
                               href="{{ path('commande_new') }}"> Prendre ce cadeau</a>
                            <a class="btn btn-default" href="{{ path('produit_show', { 'id': produit.id }) }}">Voir le produit</a>
                        </a>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>

{% endblock %}